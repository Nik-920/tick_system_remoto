name: Issues

on:
  issues:
    types: [labeled]

permissions:
  issues: write

jobs:
  help-wanted:
    # OWASP/SonarCloud: Evitar referenciar ramas mutables como "main" o "master"
    # Reemplaza el SHA por el commit real y seguro que deseas usar de esa acción.
    uses: laravel/.github/.github/workflows/issues.yml@b6f4cc920b7ba40fa0fa82cd93dd09d43ed0b5b2
