export function show(message, type = 'info') {
    // Ejemplo mínimo: puedes sustituir por un componente real (SweetAlert, Toastify, etc.)
    console.log(`[alert:${type}] ${message}`);
}

export default { show };
