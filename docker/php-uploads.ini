; PHP upload limits — INCIDEX
; Allows up to 10 MB per file with up to 5 files per request.
; post_max_size is set high enough to handle 5 × 10 MB + overhead.
upload_max_filesize=10M
post_max_size=60M
memory_limit=256M
max_file_uploads=20
